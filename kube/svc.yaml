apiVersion: apps/v1
kind: Service
metadata:
  name: hello-kubernetes-service
spec:
  selector:
    app: hello-kubernetes
  ports:
    - protocol: TCP
      port: 80        # External port to access the service
      targetPort: 3000 # Port the container listens on
  type: LoadBalancer   # Exposes the service using a LoadBalancer

